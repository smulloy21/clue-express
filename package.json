{
  "name": "clue",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "dev:db": "docker compose up -d",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --workspace=@clue/server\" \"npm run dev --workspace=@clue/client\""
  },
  "devDependencies": {
    "@eslint/js": "^9.16.0",
    "concurrently": "^10.0.4",
    "eslint": "^9.16.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.0"
  }
}
